#ifndef libmatematica
#define libmatematica

// Calcula o seno de um ângulo em radianos.
float calcular_seno(float angulo_radianos);

// Calcula o cosseno de um ângulo em radianos.
float calcular_cosseno(float angulo_radianos);

// Calcula a tangente de um ângulo em radianos.
float calcular_tangente(float angulo_radianos);

#endif